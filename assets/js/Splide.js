new Splide( '.splide', {
    type   : 'loop',
    perPage : 2,
    autoplay: true,
    interval: 3000,
    padding: {
        right: '5rem',
        left : '5rem',
    },
    height: 1000,
    fixedWidth: 500,
} ).mount();
